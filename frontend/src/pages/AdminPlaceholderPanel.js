import React from "react";

const AdminPlaceholderPanel = ({ title }) => (
  <div className="admin-placeholder">
    <h2>{title}</h2>
    <p>This section is not wired yet.</p>
  </div>
);

export default AdminPlaceholderPanel;
